# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# Python (apps/voice-agent)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Flutter (apps/voice-client)
.dart_tool/
.flutter-plugins-dependencies
.flutter-plugins
.packages
.pub-cache/
.pub/
build/
ephox/
*.iml
.metadata
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
macos/Pods/
macos/Flutter/ephemeral/
android/.gradle/
android/app/build/
android/local.properties
android/key.properties

# LiveKit / OpenVox runtime
.openvox/
.openvox-backup-*/
.claude-task-brief.md

# Tooling tmp
*.log

# === Monorepo 落地时被发现的产物（2026-07-23 清理）===
# iOS / macOS CocoaPods（每台机器 pod install 重建，不要入仓）
# Already covered above as ios/Pods/ and macos/Pods/; the **/ variants are
# belt-and-suspenders for any sibling app that has its own Pods/ tree.
**/Pods/

# Claude Code worktree 残留（开发机本地临时，不入仓）
**/.claude/worktrees/
