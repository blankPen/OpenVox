# LiveKit × Hermes API Server — 实施任务书（v2）

> **重大更新**：Hermes 自带 OpenAI 兼容 HTTP server（`gateway/platforms/api_server.py`）。
> 端口 8642，端点 `/v1/chat/completions`，支持 `X-Hermes-Session-Id` header 做房间→session 映射。
> **不再写 hermes_bridge**，直接配置 LiveKit `openai.LLM(base_url="http://127.0.0.1:8642/v1")` 即可。
>
> 启动方式：
> ```bash
> export API_SERVER_ENABLED=true API_SERVER_KEY=<secret> API_SERVER_PORT=8642
> hermes gateway run   # 前台 / WSL / Docker / Termux 推荐
> # 或者 hermes gateway start（launchd/systemd 后台）
> ```

## 项目背景

仓库：`/Users/pz/workspace/livekit`（git 切到 `feature/hermes-bridge` 分支，HEAD = c3508aa）。
当前 LiveKit Agents worker 用火山引擎 STT/LLM/TTS。
目标：把 pipeline 模式的 LLM 替换为 Hermes（通过 `http://127.0.0.1:8642/v1`），
同时清理掉 workspace/persona/skills/mcp 那套冗余能力。

## 已确认的环境事实

- Python venv：`/Users/pz/workspace/livekit/.venv/bin/python`（已激活）
- pytest 9.1.1
- fastapi/uvicorn/httpx 已装（之前 task 残留，无所谓）
- `livekit-plugins-openai==1.6.4` 待装
- 当前 `.env`：`PIPELINE=qwen-realtime`
- `~/.hermes/hermes-agent/venv/bin/hermes` CLI 可用
- `hermes gateway run` 会启动内置 api_server platform
- 当前没有任何 hermes-bridge 代码（已 reset 干净）

## 设计文档

飞书：https://poizon.feishu.cn/docx/LnFedasEGooweCxLYW2cGNY3ncf
（v1 文档里讲 hermes_bridge 实现，**v2 直接用方案 B**，详见本文档）

## 任务分解（3 个独立阶段，按顺序执行）

### Task 1 — LiveKit main.py 集成 openai.LLM 到 pipeline

**修改文件**：
- `main.py` —— `_build_session()` 只保留 `pipeline` 分支，删 `realtime` 和 `qwen-realtime`
- `.env` —— `PIPELINE=pipeline`，新增 `HERMES_BRIDGE_*` 配置（**注意命名沿用，方便后续回滚**
  —— 但实际是直接指向 hermes api_server）
- `pyproject.toml` —— 加 `livekit-plugins-openai==1.6.4`

**约束**：
- 只保留一个 PIPELINE：`pipeline`（其它分支全删）
- LLM 配置：
  ```python
  openai.LLM(
      model=os.environ.get("HERMES_BRIDGE_MODEL", "hermes-agent"),
      api_key=os.environ["HERMES_BRIDGE_API_KEY"],   # 来自 API_SERVER_KEY
      base_url=os.environ["HERMES_BRIDGE_BASE_URL"],  # 默认 http://127.0.0.1:8642/v1
  )
  ```
- STT/TTS 保留火山引擎（用户没说要换）
- 暂时**不**实现 `extra_headers={"X-Hermes-Session-Id": room_name}` —— 等 Task 2/3 端到端测试时再加

**严格 TDD 要求**：
1. RED：`tests/test_main_build_session.py` 测试 `_build_session()`：
   - `test_pipeline_uses_openai_llm`：mock `livekit.plugins.openai.LLM` 验证 base_url/api_key/model 来自环境变量
   - `test_pipeline_uses_volcengine_stt_tts`：验证 STT/TTS 仍是火山
   - `test_unknown_pipeline_raises`：传未知 PIPELINE 抛 ValueError
   - `test_qwen_realtime_branch_removed`：import 不应包含 qwen
   - `test_volcengine_realtime_branch_removed`：realtime 分支不存在
2. **RED 验证**：跑 pytest 看失败（main.py 还没改）
3. GREEN：改 main.py
4. **GREEN 验证**：全部 PASS
5. REFACTOR：清理 import

**关键**：
- 不要 mock 整个 _build_session 函数，只 mock openai.LLM 构造
- `.env` 改 PIPELINE 时用精确替换，别覆盖整个文件

**验收**：
```bash
pytest tests/test_main_build_session.py -v  # PASS
pytest tests/ -q  # 无回归
```

### Task 2 — 清理 workspace/persona/skills/mcp 那套冗余能力

**移除**：
- `main.py` 里的 `build_agent()` 函数
- `agent_persona/agent_skills/agent_extensions/agent_memory` 相关 import 和 sys.path 注入
- `WORKSPACE_ROOT` 模块级变量
- `_session_holder` 模块级变量
- `workspace/` 目录（清空或删整个目录，看 git 跟踪情况）
- `on_enter()` 里的 `_session_holder[0] = self.session`
- `from agent_persona import load_persona` 等所有 agent_* 引用

**保留**：
- `_custom_text_input_cb`（文本输入回调）
- STT / LLM / TTS 三段管线
- `prewarm_fnc` / `entrypoint` 主流程
- `VolcengineAgent`（简化版，不再接受 tools/mcp_servers）
- 开场白逻辑

**简化后的 `VolcengineAgent`**：
- 只接受 `instructions` 参数
- `on_enter` 只在 pipeline 模式下调 `generate_reply()`

**严格 TDD 要求**：
1. RED：`tests/test_volcengine_agent.py`：
   - `test_agent_instructions_default`：默认 instructions 是中文小语
   - `test_agent_instructions_override`：传 instructions 能覆盖
   - `test_on_enter_pipeline_calls_generate_reply`：pipeline 模式 on_enter 触发 generate_reply
   - `test_no_build_agent_function`：main.py 不再有 build_agent 函数
   - `test_no_agent_persona_import`：main.py 不再 import agent_persona
2. GREEN：删冗余代码
3. REFACTOR

**验收**：
```bash
pytest tests/test_volcengine_agent.py -v  # PASS
pytest tests/ -q  # 无回归
grep -r "agent_persona\|agent_skills\|agent_extensions\|agent_memory\|_session_holder\|build_agent" main.py  # 无输出
```

### Task 3 — 端到端 smoke + 文档

**目标**：验证三端联通（hermes gateway + api_server + LiveKit Worker）。

**实现**：
- `scripts/start-hermes-gateway.sh`：bash 启动 hermes gateway 并启用 api_server
- `tests/test_e2e_smoke.py`：
  - 启动 hermes gateway（subprocess，环境变量 API_SERVER_ENABLED=true 等）
  - 等待 /health 返回 200
  - POST /v1/chat/completions 发一个简单问题
  - 验证 SSE 流返回非空文本
- 更新 `README.md`：新增"PIPELINE=pipeline (Hermes)"一节

**严格 TDD 要求**：
1. RED：先写 e2e smoke 测试，跑失败（gateway 没起）
2. GREEN：实现启动脚本 + 验证 e2e 联通

**关键约束**：
- e2e 测试**必须真的启 hermes gateway**，不能 mock
- 如果 hermes gateway 因为环境问题启动失败，**老实报告 stderr**，不要伪造响应
- e2e 测试要能在 ~60s 内完成（设 timeout）

**端到端验收**（必须实际执行）：
```bash
# 启动 hermes gateway（后台）
API_SERVER_ENABLED=true API_SERVER_KEY=test123 API_SERVER_PORT=8642 hermes gateway run &
sleep 10

# 验证
curl http://127.0.0.1:8642/health
# 期望：{"ok": true, ...}

curl -N -X POST http://127.0.0.1:8642/v1/chat/completions \
  -H "Authorization: Bearer test123" \
  -H "Content-Type: application/json" \
  -d '{"model":"hermes-agent","messages":[{"role":"user","content":"用一句话介绍你自己"}],"stream":true}'
# 期望：SSE 流，包含 data: {"choices":[{"delta":{"content":"..."}}]} + data: [DONE]
```

**禁止伪造任何 curl 输出**。如果 hermes 启不起来，贴 stderr 给我。

## TDD 总原则

- **绝对禁止**：先写代码再补测试
- **每个 RED → GREEN 转换必须真的运行 pytest 看到失败 → 通过**
- **不要 mock 整套 _build_session / hermes gateway**
- **小步前进**

## 提交策略

每个 Task 完成后：
```bash
git add -A
git commit -m "task-N: <描述>"
```

## 报告要求

每个 Task 完成报告：
1. `git log --oneline -3`
2. `git diff --stat HEAD~1`
3. pytest 真实输出
4. 任何卡住的点（如果 hermes gateway 启不来，贴 stderr）

**禁止伪造任何命令输出。**